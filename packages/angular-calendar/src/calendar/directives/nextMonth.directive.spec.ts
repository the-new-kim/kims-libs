import { NextMonthDirective } from './nextMonth.directive';

describe('NextMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new NextMonthDirective();
    expect(directive).toBeTruthy();
  });
});

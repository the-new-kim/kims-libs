import { CalendarStateDirective } from './calendarState.directive';

describe('CalendarStateDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarStateDirective();
    expect(directive).toBeTruthy();
  });
});

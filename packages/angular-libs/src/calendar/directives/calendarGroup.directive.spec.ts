import { CalendarGroupDirective } from './calendarGroup.directive';

describe('CalendarGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarGroupDirective();
    expect(directive).toBeTruthy();
  });
});

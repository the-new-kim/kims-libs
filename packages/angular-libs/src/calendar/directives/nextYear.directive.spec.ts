import { NextYearDirective } from './nextYear.directive';

describe('NextYearDirective', () => {
  it('should create an instance', () => {
    const directive = new NextYearDirective();
    expect(directive).toBeTruthy();
  });
});

import { CalPrevMonthDirective } from './calPrevMonth.directive';

describe('CalPrevMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new CalPrevMonthDirective();
    expect(directive).toBeTruthy();
  });
});

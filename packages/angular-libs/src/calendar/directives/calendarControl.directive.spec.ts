import { CalendarControlDirective } from './calendarControl.directive';

describe('CalendarControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarControlDirective();
    expect(directive).toBeTruthy();
  });
});

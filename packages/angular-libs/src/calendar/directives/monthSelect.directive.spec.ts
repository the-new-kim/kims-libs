import { MonthSelectDirective } from './monthSelect.directive';

describe('MonthSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthSelectDirective();
    expect(directive).toBeTruthy();
  });
});

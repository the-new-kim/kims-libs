import { OldCalendarDirective } from './oldCalendar.directive';

describe('OldCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new OldCalendarDirective();
    expect(directive).toBeTruthy();
  });
});

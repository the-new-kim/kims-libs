import { CustomModelDirective } from './customModel.directive';

describe('CustomModelDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomModelDirective();
    expect(directive).toBeTruthy();
  });
});
